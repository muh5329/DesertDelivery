extends "res://tests/jeep_tests.gd"
## The cargo truck is gone; the Jeep took over every role it had. This name stays so the suite
## lists that still say `--test=truck_tests` run the Jeep's suite (tests/jeep_tests.gd).
